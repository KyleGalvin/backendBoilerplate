import * as assert from "assert";
import { suite, test, slow, timeout } from "mocha-typescript";

@suite class AuthControllerTests {
    @test async CRUD() {


      assert(false, "need to make all the tests");

    }
}